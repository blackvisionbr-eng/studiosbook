import React from "react";
import ReactDOM from "react-dom/client";
import AuthActionApp from "./AuthActionApp";
import "../index.css";

ReactDOM.createRoot(document.getElementById("auth-action-root")).render(
  <React.StrictMode>
    <AuthActionApp />
  </React.StrictMode>
);
